# Frontend

React + TypeScript + Vite. Ver o `README.md` na raiz do projeto para instruções completas de
setup e execução (backend + frontend).

Resumo:

```bash
npm install
cp .env.example .env   # VITE_API_URL, default http://localhost:8000
npm run dev
```
